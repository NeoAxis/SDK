//Caching files in the memory to better performance.
//The list of file extensions that will be cached by means engine virtual file system.
cachingExtensions
{
	item0 = .cg_hlsl
	item1 = .shaderBaseExtension
}
